#pragma once

#include "real.h"

namespace Absorption {

	const real Vacuum = static_cast<real>( 0.0 );
	const real Air = static_cast<real>( 0.0001 );
	const real EvaporatedWater = static_cast<real>( 0.10 );
	const real Water = static_cast<real>( 0.40 );
	const real Glass = static_cast<real>( 0.15 );

}
