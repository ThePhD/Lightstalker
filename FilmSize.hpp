#pragma once

#include "Math.hpp"

namespace FilmSize {

	const vec2 mm35Academy = vec2( real(22), real(16) );
	const vec2 mm35Widescreen = vec2( real(21.35), real(18.6) );
	const vec2 mm35CinemaScope = vec2( real( 21.95 ), real( 18.6 ) );
	const vec2 mm35Super35 = vec2( real( 24.89 ), real( 18.66 ) );
	const vec2 mm35Techniscope = vec2( real( 22 ), real( 9.47 ) );
	const vec2 FullFrame = vec2( real( 24 ), real( 36 ) );
	const vec2 APSH = vec2( real( 28.7 ), real( 19 ) );
	const vec2 Nikon1CX = vec2( real( 13.2 ), real( 8.8 ) );
	const vec2 APSCNikon = vec2( real( 23.6 ), real( 15.7 ) );
	const vec2 APSCCanon = vec2( real( 22.2 ), real( 14.8 ) );

}